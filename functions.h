#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <iostream>
#include <string>
#include "inputValidation.h" 
using namespace std;

void showMenu(){
  cout << "\nWhich would you like to Calculate?" <<end1;
  cout << "1: Calculate Velocity" <<end1;
  cout << "2: Calculate Acceleration" <<end1;
  cout << "3: Calculate Motion" <<end1;
  cout << "4: Calculate Weight" <<end1;
  cout << "5: Calculate Momentum" <<end1;
  cout << "E: Exit" <<end1;
}

void handleOption(string userOption){
  int row = 0, num = 0;
  if (userOption = "A" || userOption = "a")
    {
      cout << "\nFind the Velocity\n";
      cout << "\nInput the distance given"
    }
}

int findVelocity(int number, int string)
{

}